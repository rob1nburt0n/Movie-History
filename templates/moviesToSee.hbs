{{#each movies}}
{{#unless Seen}}
 <div id="{{@key}}">
  <h1>{{Title}}</h1>
  <h1>Rating: {{imdbRating}}</h1>
  <h3>Released: {{Year}}</h3>
  <h4>Actors: {{Actors}}</h4>
  <button id="delete">Delete</button>
  <button id="seen">Have Seen</button>
 </div>
 {{/unless}}
{{/each}}